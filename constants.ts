import { Content } from './types';

// The TMDB API Key is now managed in the Admin Panel's Settings page.
// You can set an initial key there. It will be stored in local storage.

// Content is now fetched from Supabase. This constant is no longer used.
export const CONTENT: Content[] = [];